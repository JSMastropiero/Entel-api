# from django.urls import path, include
# from rest_framework import routers
# from .views import TypeOfUserViewSet, UserViewSet, PlaceViewSet, TypeOfStatusViewSet, StatusViewSet, StatusChangeLogViewSet, IncidenceViewSet, FranchiseViewSet, StoreViewSet

# router = routers.DefaultRouter()
# router.register(r'type-of-users', TypeOfUserViewSet)
# router.register(r'users', UserViewSet)
# router.register(r'places', PlaceViewSet)
# router.register(r'type-of-statuses', TypeOfStatusViewSet)
# router.register(r'statuses', StatusViewSet)
# router.register(r'status-change-logs', StatusChangeLogViewSet)
# router.register(r'incidences', IncidenceViewSet)
# router.register(r'franchises', FranchiseViewSet)
# router.register(r'stores', StoreViewSet)

# urlpatterns = [
#     path('', include(router.urls)),
# ]